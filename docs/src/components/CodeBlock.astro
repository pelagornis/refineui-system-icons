---
interface Props {
  code: string;
}

const { code } = Astro.props;
---

<pre
  class="m-0 overflow-x-auto rounded-refineui-large bg-refineui-alias-surface-sunken p-refineui-size-medium font-mono text-sm leading-relaxed text-refineui-alias-foreground-primary"><code>{code}</code></pre>
